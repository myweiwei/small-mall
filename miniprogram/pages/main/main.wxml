
<view class="my-container">
  <swiper indicator-dots="{{indicatorDots}}" 
          autoplay="{{autoplay}}" 
          circular="{{circular}}" 
          vertical="{{vertical}}" 
          interval="{{interval}}" 
          duration="{{duration}}" 
          previous-margin="{{previousMargin}}rpx" 
          next-margin="{{nextMargin}}rpx"
          class="swiper">
      
      <block wx:for='{{images}}' wx:key='index'>
        <swiper-item>
          <image src='{{item}}' mode="widthFix" class='swiper-item-images'  />
        </swiper-item>
      </block>

  </swiper>

  <view class="products">
   
    <view class="recommend">
      <view class="line"></view>
      <image src="../../images/hot_title.png" class="recommend-image"></image>
      <view>店长推荐</view>
      <view class="line"></view>
    </view>
   
 
      <view class="products-parent">

        <block wx:for="{{products}}" wx:key="key" data-id="{{item.id}}"> 
          <view class="products-child">
            <image class="product-image" src="http://{{item.picture}}"></image>
            <view class="product-name">{{item.name}} <text class="yuan yuan-color">￥</text>
            <text class="yuan-color">{{item.price}}</text><text class="yuan">/{{item.unit}}</text></view>
            <view class="product-keywords">{{item.searchKey}}</view>
          </view>
         
          </block>
        </view>
        
   
  </view>
</view>